# Sofia OS v99 — capa padrão no Modo default

- O botão **Modo default** continua limpando conteúdo, template e ícone personalizado.
- A página não fica mais sem capa: o preset **Lavanda** (`#d9d2ff → #b8aaff`) passa a ser a capa padrão.
- Páginas novas também partem dessa capa padrão, inclusive criações feitas pela Sofia e subpáginas.
- **Remover capa** continua funcionando quando a pessoa quiser uma página sem capa.
- Mantida a correção da v98 que impede o envio do campo legado `template_id`.
