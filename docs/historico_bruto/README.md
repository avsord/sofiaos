# Histórico bruto preservado

Este diretório não deve ser apagado por rotinas de simplificação.

## Conteúdo

- `documentacao_projeto_v103/`: cópia exata dos 57 arquivos que estavam em `projeto/docs` na v103.
- `documentacao_raiz_v103/`: cópia exata dos documentos de raiz da v103 (`LEIA_Vxx`, `Comando atual.md`, prompt de continuação e PDF).
- `testes_legados_v103/`: cópia dos testes versionados antigos, mantidos como documentação executável/histórica; eles não fazem parte da suíte ativa desde a v106.
- `snapshots_v104/`: snapshots dos documentos do primeiro master simplificado.
- `snapshots_v105/`: snapshots dos documentos da v105.
- `snapshots_v106/`: snapshots do master que restaurou a documentação bruta, incluindo manifestos e relatório de testes da versão.
- `snapshots_v107/`: documentos atuais da v107 e a captura bruta do bug visual do sino reportado na v105.
- `testes_arquivados_local/`: destino futuro para testes versionados antigos encontrados numa instalação local. A ferramenta de organização move para cá em vez de apagar.

## Regra

O master atual pode ser enxuto; o histórico bruto não pode ser destruído. Em caso de dúvida sobre uma decisão antiga, procure aqui antes de alterar a arquitetura.

- `snapshots_v108/`: documentos atuais da v108, incluindo a mudança estrutural que limpou páginas normais e subpáginas no estilo Notion.

- `snapshots_v110/`: documentos da v110, com restauração da capa/ícones e placeholders apenas no hover.

- `snapshots_v111/`: correção pontual de espaçamento entre controles de bloco e texto.

- `snapshots_v118/`: documentação da reorganização da Agenda como calendar viewer.

- `snapshots_v119/`: documentação da Agenda interativa, vínculo de origem e preparação para sync de calendário externo.

  - v119 também preserva a captura bruta do formulário de Tarefa que motivou a rediagramação.

- `snapshots_v122/`: documentação da correção da barra de scroll lateral persistente no padrão Notion.
